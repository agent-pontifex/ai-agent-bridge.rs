# Rust build output
/target
target/
**/*.rs.bk

# We DO commit Cargo.lock — this is a binary/service crate and the deploy builds
# with `cargo build --release --locked`.

# Local env / secrets
.env
.env.*
!.env.example

# Editor / OS cruft
.DS_Store
*.swp
*~
.idea/
.vscode/

# Logs
*.log

# Browser automation output
tests/browser/node_modules/
tests/browser/test-results/
tests/browser/playwright-report/
tests/browser/blob-report/

# --- hardened baseline (auto) ---
Thumbs.db
tmp/
.direnv/
*.pdb
.cargo-target-*/
**/.gradle/
tmp
temp
tmp/worktrees
temp/worktrees

# Playwright browser contract (tests/blockkit) — generated only
tests/blockkit/node_modules/
tests/blockkit/artifacts/
tests/blockkit/playwright-report/
tests/blockkit/test-results/
tests/blockkit/package-lock.json

# --- environment / secrets (sops + age) -------------------------------------
# Only env/enc/*.env.enc is tracked. Every plaintext form stays out of git;
# `just env-check` enforces it. See env/README.md.
*.env
**/*.env
env/dec/

# age private keys — never commit these.
*.agekey
*.age-key
keys.txt
sops-private*.txt
