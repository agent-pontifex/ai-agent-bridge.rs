[package]
name = "agent-pontifex-protocol"
version = "0.1.0"
edition = "2021"
publish = false
description = "Vendor-neutral wire contracts shared by AI agent bridges, coordinators, and clients"
license = "MIT"
repository = "https://github.com/agent-pontifex/agent-sdk.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
